{
  "name": "openclaw-template",
  "version": "1.0.0",
  "private": true,
  "description": "OpenClaw on Render or Railway — one-click deploy",
  "scripts": {
    "start": "./scripts/setup.sh",
    "dev": "docker compose up --build",
    "dev:stop": "docker compose down",
    "dev:clean": "docker compose down -v",
    "dev:logs": "docker compose logs -f",
    "dev:shell": "docker compose exec openclaw bash",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "express": "^4.21.0",
    "http-proxy": "^1.18.1",
    "openclaw": "latest"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.0.18",
    "supertest": "^7.2.2",
    "vitest": "^4.0.18"
  }
}
